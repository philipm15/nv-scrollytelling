import { defineConfig } from 'vite';

export default defineConfig({
  // Use relative paths for assets so it works automatically on GitHub Pages 
  // regardless of what you name your repository.
  base: './', 
});
